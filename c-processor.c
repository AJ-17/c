#include <stdio.h>//<> are used for standard system directory
#include "po.c"//"" are used for current working director
#include </home/ak/learn/c/po.c> // we can specify the path too
//to add your own file in standard system directory add the file in /usr/include
#define PI 3.14 // we can replace all PI in code by 3.14 they are called pre-processor variables
#define debug
#define square(x) x*X //macro fnx
#undef PI //to undefine a macro or pre-processor variabel
#ifdef debug//true when debug is defined 
#ifndef PI//true when PI not defined
#if
#else
#pragma //to issue some special commands to the compiler


