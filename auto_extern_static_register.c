#include <stdio.h>
//#include "po.c" // if we don't include the anothr fule then we have to use extern and gcc 1.c 2.c -o main 
//if we include it then there is no need to use extern int ;

int main()

//storages class 
//auto
//exterm  will discuss in these file
//static disscused in seprate file 
//register u know :) for fast assecessing local variables

{
    //extern int a; does nothing if we included the file
    //int a;

    printf("%d\n",a);
    //auto int a is same as int a by default local varibale are called auto 
    
    // Declaration - Telling the compiler about the variable (No space reserved)
    // Definition - Declaration + space reservation


}